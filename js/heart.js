// ********** Gallery Photo 1 ************
// Select the container and heart elements from the DOM
const gallery = document.querySelector('.gallery-img'),
  heart = document.querySelector('.heart')

// Add a double-click event listener to the container
gallery.addEventListener('dblclick', (e) => {
  // Add the active class to the heart element to animate it
  heart.classList.add('active')

  // Remove the active class after 1 second
  setTimeout(() => {
    heart.classList.remove('active')
  }, 1000)
})

// ********** Gallery Photo 2 ************
// Select the container and heart elements from the DOM
const gallery2 = document.querySelector('.gallery-img2'),
  heart2 = document.querySelector('.heart2')

// Add a double-click event listener to the container
gallery2.addEventListener('dblclick', (e) => {
  // Add the active class to the heart element to animate it
  heart2.classList.add('active')

  // Remove the active class after 1 second
  setTimeout(() => {
    heart2.classList.remove('active')
  }, 1000)
})

// ********** Gallery Photo 3 ************
// Select the container and heart elements from the DOM
const gallery3 = document.querySelector('.gallery-img3'),
  heart3 = document.querySelector('.heart3')

// Add a double-click event listener to the container
gallery3.addEventListener('dblclick', (e) => {
  // Add the active class to the heart element to animate it
  heart3.classList.add('active')

  // Remove the active class after 1 second
  setTimeout(() => {
    heart3.classList.remove('active')
  }, 1000)
})

// ********** Gallery Photo 4 ************
// Select the container and heart elements from the DOM
const gallery4 = document.querySelector('.gallery-img4'),
  heart4 = document.querySelector('.heart4')

// Add a double-click event listener to the container
gallery4.addEventListener('dblclick', (e) => {
  // Add the active class to the heart element to animate it
  heart4.classList.add('active')

  // Remove the active class after 1 second
  setTimeout(() => {
    heart4.classList.remove('active')
  }, 1000)
})

// ********** Gallery Photo 5 ************
// Select the container and heart elements from the DOM
const gallery5 = document.querySelector('.gallery-img5'),
  heart5 = document.querySelector('.heart5')

// Add a double-click event listener to the container
gallery5.addEventListener('dblclick', (e) => {
  // Add the active class to the heart element to animate it
  heart5.classList.add('active')

  // Remove the active class after 1 second
  setTimeout(() => {
    heart5.classList.remove('active')
  }, 1000)
})

// ********** Gallery Photo 6 ************
// Select the container and heart elements from the DOM
const gallery6 = document.querySelector('.gallery-img6'),
  heart6 = document.querySelector('.heart6')

// Add a double-click event listener to the container
gallery6.addEventListener('dblclick', (e) => {
  // Add the active class to the heart element to animate it
  heart6.classList.add('active')

  // Remove the active class after 1 second
  setTimeout(() => {
    heart6.classList.remove('active')
  }, 1000)
})

// ********** Gallery Photo 7 ************
// Select the container and heart elements from the DOM
const gallery7 = document.querySelector('.gallery-img7'),
  heart7 = document.querySelector('.heart7')

// Add a double-click event listener to the container
gallery7.addEventListener('dblclick', (e) => {
  // Add the active class to the heart element to animate it
  heart7.classList.add('active')

  // Remove the active class after 1 second
  setTimeout(() => {
    heart7.classList.remove('active')
  }, 1000)
})

// ********** Gallery Photo 8 ************
// Select the container and heart elements from the DOM
const gallery8 = document.querySelector('.gallery-img8'),
  heart8 = document.querySelector('.heart8')

// Add a double-click event listener to the container
gallery8.addEventListener('dblclick', (e) => {
  // Add the active class to the heart element to animate it
  heart8.classList.add('active')

  // Remove the active class after 1 second
  setTimeout(() => {
    heart8.classList.remove('active')
  }, 1000)
})
